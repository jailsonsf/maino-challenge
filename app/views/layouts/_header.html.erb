<header id="main-header" class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to("Mainô", root_path, class: "navbar-brand") %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to("Home", root_path, class: "nav-link active") %>
        </li>
        <li class="nav-item">
          <%= button_to("Adicionar arquivos", new_document_path, data: { turbo: "false" }, class: "btn btn-primary", method: :get) %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <div class="nav-link active"><%= current_user.email %></div>
        </li>
        <li class="nav-item">
          <%= button_to("Sair", destroy_user_session_path, data: { turbo: "false" }, class: "nav-link active", method: :delete) %>
        </li>
      </ul>
    </div>
  </div>
</header>